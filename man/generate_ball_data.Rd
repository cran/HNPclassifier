% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hnp_package.r
\name{generate_ball_data}
\alias{generate_ball_data}
\title{Generate Synthetic Three-class Ball Data}
\usage{
generate_ball_data(n, centers, radii)
}
\arguments{
\item{n}{Integer. Number of samples to draw for each class.}

\item{centers}{A list of three numeric vectors of length 3, giving the
center coordinates of the balls for classes "A", "B", and "C".}

\item{radii}{Numeric vector of length 3 giving the radius of each class ball.}
}
\value{
A data.frame with feature columns \code{x1}, \code{x2}, \code{x3} and a factor
column \code{y} with levels c("A","B","C"), containing \code{3 * n} rows.
}
\description{
Generate a synthetic three-class dataset in which each class is
sampled uniformly from a 3-dimensional ball with a given center and radius.
Useful for demonstrating and testing the HNP Umbrella pipeline.
}
\examples{
set.seed(123)
data <- generate_ball_data(
  n = 100,
  centers = list(c(0, 0, 0), c(2, 2, 2), c(4, 0, 0)),
  radii = c(1, 1, 1)
)
}

# HNPclassifier 0.2.1

## New features

* Exported `base_function()` to train built-in base learners (random forest,
  SVM, or logistic regression) for use in the H-NP umbrella pipeline or as
  pretrained scoring models.
* Added package documentation and generated help for `base_function()` so it is
  included in the uploaded package API.

